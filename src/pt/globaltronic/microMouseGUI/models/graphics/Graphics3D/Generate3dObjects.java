package pt.globaltronic.microMouseGUI.models.graphics.Graphics3D;
import java.awt.Color;

public class Generate3dObjects {


    static int mapSize = 16;
    static double Size = 10;
    static Color G = new Color(155, 155, 155);

    public Generate3dObjects() {
        //generate the grid used for tests
        /*
        for (int x = 0; x < 16; x++) {
            for (int y = 0; y < 16; y++) {
                Screen.Polygon2DS.add(new Polygon2D(new double[]{x * Size, (x +1)* Size , (x+1) *Size, x* Size}, new double[]{y * Size, y* Size , (y+1) *Size, (y+1)* Size}, new double[]{0, 0, 0, 0}, Color.GRAY, false));
            }
        }

        //generate the wall graphics
        for (int x = 0; x < 16; x++) {
            for (int y = 0; y < 17; y++) {
                Screen.Cubes.add(new Cube(x * Size, y * Size, 0, Size, 0.2, 1, Color.BLACK));
            }
        }
        for (int x = 0; x < 17; x++) {
            for (int y = 0; y < 16; y++) {
                Screen.Cubes.add(new Cube(x * Size, y * Size, 0, 0.2, Size, 1, Color.BLACK));
            }
        }



 */


    }
}
